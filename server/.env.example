# Node
TZ=UTC
PORT=3333
HOST=localhost
NODE_ENV=development

# App
LOG_LEVEL=info
APP_KEY=
APP_URL=http://${HOST}:${PORT}
BASE_URL=http://${HOST}:${PORT}

# Session
SESSION_DRIVER=cookie

# Auth
JWT_EXPIRY=30 days

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=tacticx
DB_PASSWORD=
DB_DATABASE=tacticx_dev
DB_SSL=false

# RevenueCat
REVENUECAT_WEBHOOK_SECRET=

# Admin sync endpoint
ADMIN_SYNC_TOKEN=