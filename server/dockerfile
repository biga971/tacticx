FROM node:lts-bookworm-slim AS base

# ----------------------------
# Stage 1: pnpm via corepack
# ----------------------------
RUN corepack enable && corepack prepare pnpm@latest --activate
# Allow whitelisted native build scripts in non-interactive CI builds.
ENV CI=true

# ----------------------------
# Stage 2: Install all dependencies (reproducible via lockfile)
# ----------------------------
FROM base AS deps
WORKDIR /app
COPY package.json pnpm-lock.yaml ./
RUN pnpm install --frozen-lockfile

# ----------------------------
# Stage 3: Build the application
# ----------------------------
FROM deps AS build
WORKDIR /app
COPY . .
RUN node ace build

# ----------------------------
# Stage 4: Production runtime
# ----------------------------
FROM base AS production
WORKDIR /app
ENV NODE_ENV=production

# `node ace build` copies package.json + pnpm-lock.yaml into build/.
COPY --from=build /app/build ./
RUN pnpm install --prod --frozen-lockfile

EXPOSE 3333
CMD ["node", "bin/server.js"]
